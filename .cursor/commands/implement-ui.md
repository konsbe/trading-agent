Implement UI with the `ui-developer` subagent and React standards from the `react` subagent.

Work in `web-app/ui/spog` or `web-app/ui/mfe-<name>`. Use the Stitch kit in `web-app/shared-components` (`@trading-agent/shared-components`). If a Stitch or Figma design is provided, implement kit pieces there first, then consume them from the MFE.
