Create a new trading-agent microservice using the `svc-creator` subagent and `.cursor/agents/svc-creator.md`.

Place it at `services/svc-<service-name>`. Always add package-root `README.md`, `Makefile` (`build`, `test`, `run`, `docker`), and `Dockerfile`.
